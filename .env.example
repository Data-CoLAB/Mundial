# ── Client config (PUBLIC — ships in the browser bundle) ──
# In Vercel set these for Production + Preview. Safe to expose.
VITE_FIREBASE_API_KEY=AIzaSyB0i6_atww4X9-TPQljjzMAR_npApBGeow
VITE_FIREBASE_AUTH_DOMAIN=databets-d27ea.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=databets-d27ea
VITE_FIREBASE_STORAGE_BUCKET=databets-d27ea.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=692287183601
VITE_FIREBASE_APP_ID=1:692287183601:web:438304c691a736e0ad6750

# ── Server only (SECRET — used by /api functions, NEVER exposed to the client) ──
# Paste the ENTIRE JSON of a Firebase service-account key
# (Firebase Console → Project Settings → Service accounts → Generate new private key).
# In Vercel set this as a normal env var, NOT prefixed with VITE_.
FIREBASE_SERVICE_ACCOUNT=
